# Legacy path: combination queries

AshR2ML does not execute Ash queries and does not provide a query-combination engine.

Union, intersection, combination, sorting, filtering, aggregates, and calculations remain concerns of Ash and the active data layer. SPARQL algebra belongs to the selected OBDA engine.

See [Query surfaces](query-surfaces.md).

This compatibility file intentionally contains no Cypher guidance.
